services:
  cheeta:
    image: 1dev/server
    volumes:
      - ./cheeta:/opt/cheeta
      - /var/run/docker.sock:/var/run/docker.sock
    restart: always
    ports:
      - "6610:6610"
      - "6611:6611"
    environment:
      ### This should be the same as POSTGRES_PASSWORD env below
      hibernate_connection_password: "changeit"

      hibernate_dialect: io.cheeta.server.persistence.PostgreSQLDialect
      hibernate_connection_driver_class: org.postgresql.Driver
      hibernate_connection_url: jdbc:postgresql://postgres:5432/cheeta
      hibernate_connection_username: postgres
    tty: true
    depends_on:
      - "postgres"

  postgres:
    image: postgres:14-alpine3.15
    restart: always
    environment:
      ### Change this
      POSTGRES_PASSWORD: "changeit"

      POSTGRES_USER: "postgres"
      POSTGRES_DB: "cheeta"
    expose:
      - "5432"
    volumes:
      - ./cheeta/site/postgres:/var/lib/postgresql/data
